[pytest]
markers =
    smoke: fast smoke tests (quick checks)
    integration: integration tests
    requires_agent: tests that require agent execution
    heavy: slow / resource-intensive tests (e.g. dashboard, large data)